<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_management"
    app:startDestination="@id/listFragment">

    <fragment
        android:id="@+id/listFragment"
        android:name="com.feny.pokemons.ui.listing.ListingPokemonsView"
        android:label="Home"
        tools:layout="@layout/fragment_listing_pokemons" />

    <fragment
        android:id="@+id/searchFragment"
        android:name="com.feny.pokemons.ui.listing.SearchPokemonsView"
        android:label="Dashboard"
        tools:layout="@layout/fragment_search_pokemons" />

    <fragment
        android:id="@+id/favoriteFragment"
        android:name="com.feny.pokemons.ui.listing.FavoritePokemonView"
        android:label="Notifications"
        tools:layout="@layout/fragment_favorite_pokemons" />

</navigation>